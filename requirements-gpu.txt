# GPU зависимости для GOP проекта
# Установить с: pip install -r requirements-gpu.txt

# Включает основные зависимости
-r requirements.txt

# GPU ускорение для CUDA (только для Linux/Windows)
cupy-cuda11x>=12.0.0,<13.0.0; sys_platform != "darwin"

# PyTorch с CUDA поддержкой (только для Linux/Windows)
# Для CUDA 11.8
# torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cu118
# torchvision>=0.15.0,<1.0.0 --index-url https://download.pytorch.org/whl/cu118

# Для CUDA 12.1
# torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cu121
# torchvision>=0.15.0,<1.0.0 --index-url https://download.pytorch.org/whl/cu121

# Примечание: Раскомментируйте соответствующие строки выше в зависимости от вашей версии CUDA